name: Build
on:
  - push
jobs:
  get-vals:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: echo "::set-env name=INPUTS::Â$(cat inputs)" 
  something:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        val: [x, y, z]
    name: something
    steps:
    - uses: actions/checkout@v2
    - run: ./build.sh ${{ matrix.val }}
 
